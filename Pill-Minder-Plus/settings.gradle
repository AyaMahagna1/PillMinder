dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repo is going to shut down soon
    }
}
rootProject.name = "Pill Minder Plus"
include ':app'
